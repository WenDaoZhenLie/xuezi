## **类(class)**
```python
class ClassName():
    """
    类命名：大驼峰
    类属性和类方法被所有类实例所共享
    """

    # 类属性
    class_attribute = None

    # 类方法
    @classmethod
    def class_method(cls):
        """
        类方法的第一个参数表示类本身，由解释器自动绑定
        """
        pass

    # 静态方法
    @staticmethod
    def static_method(*args, **kwargs):
        pass

    # 构造方法
    def __init__(self, public_attribute):
        """
        对象方法的第一个参数表示当前对象，由解释器自动绑定
        """
        # 对象属性
        self.public_attribute = public_attribute

    # 对象方法
    def public_method(self, *parameters):
        pass
```

## **实例(instance)**
```python
obj = ClassName(*attributes)
obj.public_method(*parameters)
```

## **封装**
```python
class ClassName():
    """
    __：私有
    """
    def __init__(self, private_attribute):
        # 对象私有属性
        self.__private_attribute = private_attribute

    # 对象私有方法
    def __private_method(self,*parameters):
        pass

    # obj.private_attribute
    @property
    def private_attribute(self):
        return self.__private_attribute

    # obj.private_attribute = None
    @private_attribute.setter
    def private_attribute(self, private_attribute):
        self.__private_attribute = private_attribute
```

## **继承**
```python
class ClassName(Parent1, Parent2...):
    pass

class super(object):
    def super(cls, inst):
        mro = inst.__class__.mro()
        return mro[mro.index(cls) + 1]
    pass 
```

## **多态**

## **魔术属性&魔术方法**
```python
"""
__variable__：魔术属性/魔术方法
"""
class ClassName():
    """
    魔术属性
    __dict__：类保存属性和方法，对象保存对象属性 
    __mro__：保存当前类的继承链
    
    魔术方法
    __init__：构造方法
    """
```

## 元类
```python
class type(object):
    pass
```